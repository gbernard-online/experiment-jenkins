node {
    stage('clone') {
        checkout scm
    }

    stage('run') {
        dir('ansible') {
            ansiblePlaybook colorized: true, inventory: 'hosts', playbook: 'playbook/empty.yml'
        }
    }
}
